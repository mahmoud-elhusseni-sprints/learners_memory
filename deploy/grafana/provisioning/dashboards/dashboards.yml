apiVersion: 1
providers:
  - name: learner-memory
    folder: Learner Memory
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
